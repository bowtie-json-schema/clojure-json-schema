# clojure-json-schema

A [Bowtie](https://github.com/bowtie-json-schema/bowtie) test harness.

Its image is published to `ghcr.io/bowtie-json-schema/clojure-json-schema` and run via `bowtie run -i clojure-json-schema`.
